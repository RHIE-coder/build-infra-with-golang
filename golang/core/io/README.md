	// matadataDir := os.DirFS("../../contract")
	// files, err := os.ReadDir(basePath)
	// if err != nil {
	// 	log.Fatal(err.Error())
	// }
	// for _, f := range files {
	// 	fmt.Println(f.IsDir())
	// 	fmt.Println(f.Name())
	// 	fmt.Println(path.Ext(f.Name()))
	// }